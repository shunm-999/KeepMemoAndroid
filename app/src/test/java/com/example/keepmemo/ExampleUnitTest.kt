package com.example.keepmemo

import com.google.common.truth.Truth.assertThat
import org.junit.Test

class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertThat(4L).isEqualTo(2 + 2)
    }
}
